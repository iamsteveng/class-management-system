# Class Management System — Agent Instructions

## Before You Do Anything

Read the project structure carefully before implementing. This is a Next.js + Convex app.

## Stack
- **Frontend:** Next.js 15 (App Router), Tailwind CSS, hosted on Vercel
- **Backend:** Convex (functions, database, cron, file storage)
- **WhatsApp:** Twilio
- **Auth:** Custom admin auth (username/password, stored in Convex)

## Key Conventions
- Convex schema is in `convex/schema.ts` — update it first when adding new fields/tables
- Convex functions go in `convex/` directory (queries, mutations, actions)
- Frontend pages go in `app/` using Next.js App Router
- Admin portal is at `app/admin/`
- Participant-facing pages: `app/participant/[participant_id]/` and `app/terms/`
- Use `v.optional(...)` in Convex schema for optional fields
- Run `npx convex dev` to sync schema changes (but don't run it in implementation — just write the code)
- Tailwind for all styling — no custom CSS unless necessary

## Working on a Story
1. Check `progress.txt` for prior learnings (Codebase Patterns section)
2. Implement the story
3. Run typecheck: `npx tsc --noEmit`
4. Commit with: `feat: [Story ID] - [Story Title]`

## QA Patterns

_Auto-generated by qa-codex-loop on 2026-03-18 14:04 UTC (run: 20260318T124828Z)_

## Codebase Patterns

### Convex Deployments
- **Production (Vercel app):** `https://colorless-raven-523.convex.cloud` — used by `class-management-system-teal.vercel.app`
- **Dev/test:** `https://graceful-mole-393.convex.cloud` — used by older test files (tc001-005, tc004, tc005, etc.)
- Tests that navigate to the Vercel app MUST use `colorless-raven-523` for Convex calls (so tokens are visible to the app)
- Existing tests in `e2e/tc010.spec.ts`, `e2e/tc011.spec.ts` correctly use `colorless-raven-523`

### Branch/Deployment State
- `feat/backlog-prd` branch has extra participant fields (height, age, emergency contact) in `terms-form.tsx` and `convex/terms.ts`
- `main` branch (deployed to Vercel + Convex production) does NOT have these extra fields yet
- E2E tests for features in `feat/backlog-prd` will FAIL until the branch is merged and deployed

### Test File Location
- Playwright `testDir` is `./e2e` by default; updated to scan `./tests/e2e/` too via `testMatch` in `playwright.config.ts`
- New test format: `tests/e2e/tc-NNN.test.ts`

### Terms Page
- Token-based: `/terms?token=<uuid>` — token must exist in the Convex `purchases` table
- On success: redirects to `/terms?token=<token>&status=success`, page shows "Terms accepted successfully."
- Form has `session_id` (select), `height`, `age`, `emergency_contact_name`, `emergency_contact_phone`, `accepted` checkbox — in `feat/backlog-prd` only
- `testPurchase:createTestPurchase` creates a purchase in `pending_terms` status with a fresh UUID token

---
