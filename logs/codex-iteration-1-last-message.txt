Implemented `US-008` on `ralph/class-management-mvp` and committed with:

`61dd6ca feat: [US-008] - [Generate participants and send QR links]`

### What I changed
- Added participant generation to terms acceptance in [convex/terms.ts](/home/ec2-user/.openclaw/workspace/class-management-system/convex/terms.ts):
  - On successful terms submission, it now creates `participant_count` participant records.
  - Each participant gets a unique `participant_id` via `crypto.randomUUID()`.
  - Each participant is linked to purchase, selected session, terms version, and acceptance timestamp.
  - Schedules WhatsApp link delivery after creation.
- Added new Node action in [convex/participantLinks.ts](/home/ec2-user/.openclaw/workspace/class-management-system/convex/participantLinks.ts):
  - Sends participant page links via Twilio WhatsApp.
  - Sends all participant links in a single message (so count `2` includes both links in one message).
- Updated `US-008` to passing in [prd.json](/home/ec2-user/.openclaw/workspace/class-management-system/prd.json).
- Appended iteration log entry in [progress.txt](/home/ec2-user/.openclaw/workspace/class-management-system/progress.txt).

### Quality checks
- Ran `npm run typecheck` and `npm run lint` successfully.

### AGENTS updates
- Checked for reusable module-level learnings; no AGENTS.md update was necessary for this story.