# Ralph Progress Log
Started: Thu Mar 19 04:46:35 UTC 2026
---

## 2026-03-19 - US-001 through US-011
- Implemented all 11 user stories in a single session
- Files changed:
  - convex/schema.ts — added payment_url to classes table
  - convex/adminClasses.ts — mutations/queries support payment_url
  - convex/homepage.ts — added listClassesWithPaymentUrl query
  - convex/adminParticipants.ts — added class_id to details response, added getAvailableSessionsForClassChange query
  - convex/terms.ts — acceptTermsByToken now returns participant_id
  - app/admin/sessions/[session_id]/participants/session-participants-panel.tsx — removed Name column
  - app/admin/participants/[participant_id]/page.tsx — removed Name field, added Change Session panel
  - app/admin/participants/[participant_id]/change-session-panel.tsx — new client component for session change modal
  - app/admin/classes/page.tsx — filter by status, payment_url in actions, pass initialPaymentUrl
  - app/admin/classes/class-filter-dropdown.tsx — new client component for status filter dropdown
  - app/admin/classes/add-class-modal.tsx — payment_url field added
  - app/admin/classes/edit-class-modal.tsx — payment_url field added with initialPaymentUrl prop
  - app/api/classes/route.ts — switched to listClassesWithPaymentUrl query
  - app/homepage-client.tsx — redesigned to show class cards with Buy Ticket buttons
  - app/terms/terms-form.tsx — added instructional note before submit button
  - app/terms/page.tsx — success state redesign with green tick + QR code button

- **Learnings for future iterations:**
  - The getParticipantAdminDetails query now returns class_id (added in US-006)
  - Server actions pattern: defined inline in server components with "use server" directive, passed to client components as submitAction props
  - Class filter uses `filter` searchParam (not `status` which is used for success messages)
  - Success redirect for terms: `/terms?token=xxx&status=success&participant_id=yyy`
  - The `changeParticipantSession` mutation enforces 2-day-ahead constraint; the admin portal surfaces error messages from the mutation via URL params
---

## 2026-03-25 - US-001 through US-005 (ralph/new-homepage-integration)
- Implemented all 5 stories in this PRD in a single session

### US-001: Copy homepage components and assets
- Copied NEW_HOMEPAGE/src/ components to app/components/homepage/
- Replaced `figma:asset/<hash>.png` imports with const string literals `/images/homepage/<hash>.png`
- Updated relative import paths: `'../contexts/...'` → `'../../contexts/...'`, `'../../imports/'` → `'./imports/'`
- Added NEW_HOMEPAGE to tsconfig.json exclude list (it has vite/figma dependencies that TypeScript can't resolve)
- Added `// @ts-nocheck` to 3 shadcn UI files (calendar.tsx, chart.tsx, resizable.tsx) that have version compatibility issues
- Installed all required npm packages: clsx, class-variance-authority, tailwind-merge, lucide-react, all @radix-ui/*, recharts, sonner, vaul, etc.

### US-002: Replace page.tsx with new homepage
- Added `'use client'` to LanguageContext.tsx and ResponsiveLanding.tsx
- page.tsx is a server component that renders `<LanguageProvider><ResponsiveLanding /></LanguageProvider>`
- Deleted app/homepage-client.tsx

### US-003: Create courseConfig.ts
- app/i18n/courseConfig.ts exports COURSE_CONFIG and getCourseConfig(class_id)
- Images: cycling_fundamentals → 30c657... (beginner), city_guided_tour → 1b6dde... (tours)

### US-004: Wire CoursesSection to live backend
- /api/classes returns `{ classes: [{ class_id, name, description, payment_url }] }`
- /api/classes/[classId]/sessions returns `{ sessions: [{ session_id, date, time, location, quota_available }] }`
- Classes not in COURSE_CONFIG are skipped (getCourseConfig returns undefined)
- Enroll button is `<a href={paymentUrl}>` (not a div)
- Loading skeleton uses `animate-pulse` class (2 cards during fetch)

### US-005: Add unit and integration tests
- Added Vitest with jsdom environment for component tests
- vitest.config.ts include pattern: `tests/unit/**/*.test.{ts,tsx}` to avoid picking up Playwright e2e files
- @testing-library/react for component rendering + mocked fetch
- All 12 tests pass

- **Learnings for future iterations:**
  - NEW_HOMEPAGE directory should always be in tsconfig exclude (has figma: protocol imports + vite)
  - Vitest include pattern must be explicit to avoid picking up Playwright tests
  - `'use client'` required on LanguageContext.tsx (uses useState) and entry-point component (ResponsiveLanding)
  - shadcn/ui UI components (calendar, chart, resizable) may have version compatibility issues with latest packages — use `// @ts-nocheck`
  - Sessions API route is at /api/classes/[classId]/sessions (not /api/sessions?class_id=<id>)
---

## 2026-03-25 - US-001 (ralph/bilingual-support)
- Renamed `classes.name` → `classes.name_zh` (optional), added `name_en` (optional)
- Renamed `sessions.location` → `sessions.location_zh` (optional), added `location_en` (optional), `end_time` (optional)
- Files changed: convex/schema.ts, convex/migrations.ts (new), convex/adminClasses.ts, convex/adminSessions.ts, convex/homepage.ts, convex/participants.ts, convex/terms.ts, convex/adminParticipants.ts, convex/seed.ts
- Updated Next.js types/actions: app/admin/classes/page.tsx, sessions/page.tsx, participants pages, terms pages, CoursesSection.tsx
- Created convex/migrations.ts with `migrateToNameZhLocationZh` mutation
- **Learnings for future iterations:**
  - In Convex, removing `name`/`location` from schema and adding optional `name_zh`/`location_zh` works because existing docs with extra fields are not rejected
  - `(record as any).fieldName` pattern needed in migration to access old un-schema'd fields
  - Server actions in admin pages read formData with old field names (e.g. `formData.get("name")`) but pass to Convex as `name_zh` — form UI label changes come in US-007/008
  - All Convex return type validators must be updated alongside schema changes
  - CoursesSection.tsx has `ApiClass` and `ApiSession` interfaces that mirror the API response shape — must update these too
---

## 2026-03-25 - US-002 (ralph/bilingual-support)
- Added `description_zh` (required) and `description_en` (optional) to `CourseConfig` interface in `app/i18n/courseConfig.ts`
- Populated descriptions for `class_cycling_fundamentals` and `class_city_guided_tour`
- Updated `CoursesSection.tsx`: `Course` now stores `title_zh/title_en` and `description_zh/description_en`; `ClassSchedule` stores `location_zh/location_en/end_time`
- `CourseCard` uses `language` from `useLanguage()` to apply fallback logic at render time (EN → en ?? zh, ZH → zh)
- Time display shows `start_time–end_time` when `end_time` is present
- API routes already return bilingual fields (from US-001 schema work) — no changes needed there
- **Learnings for future iterations:**
  - Store bilingual data in state objects (title_zh/title_en etc.) and compute display strings at render time — this makes language toggle reactive without re-fetching data
  - `language === 'en'` is the correct check (Language type is `'zh-TW' | 'en'`)
  - API routes (`/api/classes` and `/api/classes/[classId]/sessions`) already return bilingual fields — the Convex queries `listClassesWithPaymentUrl` and `getAvailableSessionsByClass` in `homepage.ts` already include `name_zh`, `name_en`, `location_zh`, `location_en`, `end_time`
---
