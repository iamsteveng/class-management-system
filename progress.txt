# Ralph Progress Log
Started: Tue Mar 31 05:59:43 UTC 2026
---

## Codebase Patterns
- participants table has no `by_purchase_id` index — use `ctx.db.query("participants").filter(q => q.eq(q.field("purchase_id"), purchase._id)).first()` to get first participant for a purchase
- terms page success state is triggered by `?status=success` OR by `purchase_status === "terms_accepted"` (after US-014)

---

## 2026-03-31 - US-014
- What was implemented: Show success state when user revisits terms page after submission
- Files changed:
  - `convex/terms.ts`: Added `participant_id: v.optional(v.string())` to getTermsPageData return validator; added logic to query first participant by purchase._id when status is terms_accepted
  - `app/terms/page.tsx`: Added `participant_id?` to TermsPageData type; updated participantId to fallback to `pageData.participant_id`; changed condition from `if (submissionSucceeded)` to `if (submissionSucceeded || alreadyAccepted)`
- **Learnings for future iterations:**
  - participants table has no by_purchase_id index — use `.filter()` to query by purchase_id
  - The page already had `alreadyAccepted` variable but only used it to lock the form — just needed to also gate the success UI render on it
  - `pageData.participant_id` from Convex query provides the participant_id for the QR code button when revisiting
---

## 2026-03-31 - US-015
- What was implemented: Remove participant name field from participant pass page
- Files changed:
  - `app/participant/[participant_id]/ParticipantPageContent.tsx`: Removed `participant_name: string` from `ParticipantPageData` type (the dt/dd row had already been removed in a prior commit)
  - `app/participant/[participant_id]/page.tsx`: Removed `participant_name: string` from `ParticipantPageData` type
  - `app/i18n/participantTranslations.ts`: Removed `participantNameLabel` from the `ParticipantTranslations` interface and both 'zh-TW' and 'en' locale objects
- **Learnings for future iterations:**
  - The dt/dd row was already removed in commit 3392ae1 but the TypeScript type and i18n entries were not cleaned up — US-015 completed the cleanup
  - `convex/participants.ts` still returns `participant_name` in its validator — that's fine since the frontend just doesn't use it
---
